 <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid"> 
    <%= link_to root_path, class:"navbar-brand m-auto ps-1" do %>
      <h2 class="m-auto">Application</h2>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse lead" id="navbarSupportedContent">
      <ul class="navbar-nav m-auto">
        <li class="nav-item">
          <%= link_to "New Shot", new_shot_path, class:"nav-link active" %> 
        </li>
      </ul>
      <ul class="navbar-nav ms-auto pe-1">
          <% if !user_signed_in? %> 
            <li class="nav-item">
              <%= link_to "Sign In", new_user_session_path, class:"nav-link active" %>
            </li>
          <% else %> 
            <li class="nav-item">
              <%= link_to "#{current_user.name}", edit_user_registration_path, class:"nav-link active" %>
            </li>
            <li class="nav-item">
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class:"nav-link active" %>
            </li>
          <% end %> 
       </ul>
    </div>
  </div>
</nav>
